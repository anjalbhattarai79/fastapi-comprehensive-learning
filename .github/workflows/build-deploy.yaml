name: Build and Deploy code

# Trigger point
on : ["push", "pull_request"]

jobs:
 job1:
  runs-on: ubuntu-latest
  steps:
   - name: pulling git repo
     uses: actions/checkout@v2
     
   - name: say hi to anjal
     run: echo "Hello Anjal, Welcome to GitHub Actions"
      
  